# ── Notebook 1: transcription ─────────────────────────────────────────────
openai-whisper
pandas
ffmpeg-python          # Python bindings (ffmpeg binary must be installed separately)

# ── Notebook 2: LLM transcript query ─────────────────────────────────────
ollama
pandas

# ── Notebook 3: basketball CV pipeline ───────────────────────────────────
supervision==0.27.0
inference-gpu[sam,yolo-world]
git+https://github.com/roboflow/sports.git@feat/basketball
transformers
num2words
torch
opencv-python
numpy
Pillow
tqdm
hydra-core

# Flash Attention (CUDA 12 + PyTorch 2.5, Python 3.12):
# Install manually if needed:
# pip install https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3/flash_attn-2.8.3+cu12torch2.5cxx11abiFALSE-cp312-cp312-linux_x86_64.whl
